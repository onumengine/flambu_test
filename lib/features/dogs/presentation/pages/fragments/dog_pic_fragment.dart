import 'package:flutter/material.dart';

class DogPicFragment extends StatelessWidget {
  const DogPicFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DogPicFragment'),
    );
  }
}
